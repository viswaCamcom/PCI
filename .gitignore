# Downloaded road images — large binary files, not source code
downloaded_images/

# Environment secrets
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Logs and upload tracking
*.log
pci_uploaded_done.txt
*_uploaded_done.txt
*_failed_ids.txt
*.bak
*.bak[0-9]
*.bak_*
reprocess_checkpoint.txt

# Bulk frame CSVs — inputs and reports, not source. The two that carry
# frame_id -> image_bucket/image_key mappings (016001_municipality_frames.csv,
# 016001_retry.csv) are backed up to s3://pci-bucket/mappings/ instead; they are
# the only way to re-fetch 208k orphaned originals, so do not delete them.
/*.csv

# macOS
.DS_Store

# Docker volumes / data
mysql_data/
rabbitmq_data/

# DB dumps and generated id lists produced by deploy/copy_to_vm.sh
deploy/dump/

# Local agent config
.claude/
